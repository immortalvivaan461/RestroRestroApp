<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restro-Reservation Status</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-image: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('/img/bannert.jpg');
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }
    
        .status-card {
            background-color: rgba(255, 255, 255, 0.95);
            padding: 20px;
            border-radius: 10px;
        }
    </style>
</head>

<body class="bg-light">

    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-6">

                <!-- Check Reservation Form -->
                <div class="card shadow mb-4">
                    <div class="card-body text-center">
                        <h2 class="card-title mb-4">Check Your Reservation Status</h2>
                        <form action="/status" method="POST">
                            <div class="mb-3 text-start">
                                <label for="emailOrId" class="form-label">Email or Booking ID</label>
                                <input type="text" name="emailOrId" id="emailOrId" class="form-control"
                                    placeholder="Enter your Email or Booking ID" required>
                            </div>
                            <button type="submit" class="btn btn-success w-100">Check Status</button>
                        </form>
                    </div>
                </div>

                <!-- Display Reservation Result -->
                <% if (reservation) { %>
                    <div class="card shadow text-center">
                        <div class="card-body">
                            <h3 class="card-title mb-3">Hello <%= reservation.name %>!</h3>
                            <p>Your reservation for <strong>
                                    <%= reservation.date %>
                                </strong> at <strong>
                                    <%= reservation.timing %>
                                </strong> is currently:</p>
                            <h2 class="text-warning">
                                <%= reservation.status.toUpperCase() %>
                            </h2>
                            <p>Occasion: <%= reservation.occasion || 'N/A' %>
                            </p>
                            <p>We will update your table number on WhatsApp soon!</p>
                        </div>
                    </div>
                    <% } else if (error) { %>
                        <div class="alert alert-danger text-center mt-3">
                            <%= error %>
                        </div>
                        <% } %>

            </div>
        </div>
    </div>

</body>

</html>