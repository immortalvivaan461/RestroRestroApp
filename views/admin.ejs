<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Admin Dashboard</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 2rem;
            background-color: #f9f9f9;
        }

        h1 {
            margin-bottom: 1rem;
            text-align: center;
            color: #333;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 0.75rem;
            text-align: center;
        }

        th {
            background-color: #f0f0f0;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #f8f8f8;
        }

        button {
            padding: 0.4rem 0.8rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
        }

        .accept-btn {
            background-color: #28a745;
            color: white;
        }

        .reject-btn {
            background-color: #dc3545;
            color: white;
        }

        .status {
            font-weight: bold;
            text-transform: capitalize;
        }
    </style>
</head>

<body>
    <h1>All Reservation Requests</h1>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Email</th>
                <th>Contact</th>
                <th>Persons</th>
                <th>Food</th>
                <th>Timing</th>
                <th>Date</th>
                <th>Occasion</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% reservations.forEach((reservation, index)=> { %>
                <tr>
                    <td>
                        <%= index + 1 %>
                    </td>
                    <td>
                        <%= reservation.name %>
                    </td>
                    <td>
                        <%= reservation.email %>
                    </td>
                    <td>
                        <%= reservation.contact %>
                    </td>
                    <td>
                        <%= reservation.persons %>
                    </td>
                    <td>
                        <%= reservation.food %>
                    </td>
                    <td>
                        <%= reservation.timing %>
                    </td>
                    <td>
                        <%= reservation.date %>
                    </td>
                    <td>
                        <%= reservation.occasion %>
                    </td>
                    <td class="status">
                        <%= reservation.status %>
                    </td>
                    <td>
                        <form action="/admin/accept/<%= reservation._id %>" method="POST" style="display:inline;">
                            <button class="accept-btn" type="submit">Accept</button>
                        </form>
                        <form action="/admin/reject/<%= reservation._id %>" method="POST" style="display:inline;">
                            <button class="reject-btn" type="submit">Reject</button>
                        </form>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
</body>

</html>